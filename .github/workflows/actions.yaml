name: Approval

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  approval:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: ["RC", "PROD"]
    environment: approvers
    steps:
      - name: echo approval
        run: echo "Approved!! Huzzah!!"
      - name: echo commands
        run: echo ${{ matrix.environment }}
